1. js is object oriented, object based, structured ans prototype based language or object based because it doesn't have encapsulation
2. open menthod arguments AJAX ans:5
3. ajax use css for visually true: Ajax uses XHTML for content, CSS for presentation, along with Document Object Model and JavaScript for dynamic content display.
4. generater funtoin for of gen() console.log
5. html5 upercase tag, empty element, quotes not required, attribues no value
6. `function(x, f = () => x){ var x; var y=x; x = 2; return [x,y,f()]} function(1)` => ans:[2,1,1]
7. moveto(0,0) line(100, 100), ctx.stroke()
8. "1101011011011".split("0", 4) ans:  `["11", "1", "11", "11"]`

